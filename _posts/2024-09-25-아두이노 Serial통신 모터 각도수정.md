```python
from tkinter import *
import serial

# 아두이노와 연결할 시리얼 포트 설정
portNum = "COM3"  # 사용하고자 하는 COM 포트 번호 설정
ser = serial.Serial(portNum, 9600, timeout=1)  # 시리얼 통신: 9600bps로 시리얼 포트 열기

# 각도 그룹 저장용 리스트 (서보 4개의 각도를 저장하기 위한 리스트들)
group1 = [0, 0, 0, 0]  # 그룹 1의 서보 각도 리스트
group2 = [0, 0, 0, 0]  # 그룹 2의 서보 각도 리스트
group3 = [0, 0, 0, 0]  # 그룹 3의 서보 각도 리스트

# 라디오 버튼 선택값 저장 (1, 2, 3 값 중 하나를 가짐)
selected_group = IntVar()  # 선택된 그룹 번호 저장용 변수

# 서보 모터 각도 값을 시리얼 포트로 전송하는 함수
def send_servo_angles():
    # GUI에서 각 서보 모터의 슬라이더 값을 가져옴
    angle1 = sc1.get()  # 서보 1의 각도 값 가져오기
    angle2 = sc2.get()  # 서보 2의 각도 값 가져오기
    angle3 = sc3.get()  # 서보 3의 각도 값 가져오기
    angle4 = sc4.get()  # 서보 4의 각도 값 가져오기

    # 각도를 시리얼 포트를 통해 전송 (1번부터 4번 서보에 해당하는 값 전송)
    ser.write(f"1:{angle1}\n".encode())  # 서보 1의 각도 전송
    ser.write(f"2:{angle2}\n".encode())  # 서보 2의 각도 전송
    ser.write(f"3:{angle3}\n".encode())  # 서보 3의 각도 전송
    ser.write(f"4:{angle4}\n".encode())  # 서보 4의 각도 전송

    # 전송된 각도 값을 GUI 레이블에 표시
    lb1_val.config(text=str(angle1))  # 서보 1 각도 값 레이블 업데이트
    lb2_val.config(text=str(angle2))  # 서보 2 각도 값 레이블 업데이트
    lb3_val.config(text=str(angle3))  # 서보 3 각도 값 레이블 업데이트
    lb4_val.config(text=str(angle4))  # 서보 4 각도 값 레이블 업데이트

# 각도를 그룹 1에 저장하는 함수
def save_group1():
    # 각 서보의 현재 슬라이더 값을 그룹 1 리스트에 저장
    group1[0] = sc1.get()  # 서보 1의 각도를 그룹 1에 저장
    group1[1] = sc2.get()  # 서보 2의 각도를 그룹 1에 저장
    group1[2] = sc3.get()  # 서보 3의 각도를 그룹 1에 저장
    group1[3] = sc4.get()  # 서보 4의 각도를 그룹 1에 저장
    print(f"그룹 1 저장: {group1}")  # 그룹 1 각도 값을 콘솔에 출력

# 각도를 그룹 2에 저장하는 함수
def save_group2():
    # 각 서보의 현재 슬라이더 값을 그룹 2 리스트에 저장
    group2[0] = sc1.get()  # 서보 1의 각도를 그룹 2에 저장
    group2[1] = sc2.get()  # 서보 2의 각도를 그룹 2에 저장
    group2[2] = sc3.get()  # 서보 3의 각도를 그룹 2에 저장
    group2[3] = sc4.get()  # 서보 4의 각도를 그룹 2에 저장
    print(f"그룹 2 저장: {group2}")  # 그룹 2 각도 값을 콘솔에 출력

# 각도를 그룹 3에 저장하는 함수
def save_group3():
    # 각 서보의 현재 슬라이더 값을 그룹 3 리스트에 저장
    group3[0] = sc1.get()  # 서보 1의 각도를 그룹 3에 저장
    group3[1] = sc2.get()  # 서보 2의 각도를 그룹 3에 저장
    group3[2] = sc3.get()  # 서보 3의 각도를 그룹 3에 저장
    group3[3] = sc4.get()  # 서보 4의 각도를 그룹 3에 저장
    print(f"그룹 3 저장: {group3}")  # 그룹 3 각도 값을 콘솔에 출력

# 선택된 그룹의 각도를 서보모터에 전송하는 함수
def send_group_angles():
    group = selected_group.get()  # 선택된 그룹 번호 가져오기
    
    # 선택된 그룹에 따라 그룹의 각도 리스트를 결정
    if group == 1:
        angles = group1  # 그룹 1 각도 가져오기
    elif group == 2:
        angles = group2  # 그룹 2 각도 가져오기
    elif group == 3:
        angles = group3  # 그룹 3 각도 가져오기
    else:
        print("그룹이 선택되지 않았습니다.")  # 그룹이 선택되지 않았을 때
        return

    # 선택된 그룹의 각도를 시리얼 포트를 통해 서보모터에 전송
    ser.write(f"1:{angles[0]}\n".encode())  # 서보 1 각도 전송
    ser.write(f"2:{angles[1]}\n".encode())  # 서보 2 각도 전송
    ser.write(f"3:{angles[2]}\n".encode())  # 서보 3 각도 전송
    ser.write(f"4:{angles[3]}\n".encode())  # 서보 4 각도 전송
    
    # 전송된 각도를 GUI 레이블에 표시
    lb1_val.config(text=str(angles[0]))  # 서보 1 각도 레이블 업데이트
    lb2_val.config(text=str(angles[1]))  # 서보 2 각도 레이블 업데이트
    lb3_val.config(text=str(angles[2]))  # 서보 3 각도 레이블 업데이트
    lb4_val.config(text=str(angles[3]))  # 서보 4 각도 레이블 업데이트
    
    print(f"그룹 {group} 각도 전송: {angles}")  # 전송된 그룹 각도를 콘솔에 출력

# Tkinter 윈도우 설정
win = Tk()  # Tkinter 윈도우 객체 생성
win.geometry("700x600")  # 윈도우 크기 설정
win.title("Servo Control")  # 윈도우 제목 설정
win.option_add("*Font", "맑은고딕 20")  # 기본 폰트 설정
win.config(bg="#D1B2FF")  # 배경색 설정

# 화면 구성: 각종 레이블, 슬라이더 및 버튼 생성
lb_m = Label(win, text="모터 각도 조절")  # 모터 각도 조절 레이블
lb_m.grid(column=0, row=0, padx=10, pady=5)

btn_m_a = Button(win, text="각도 전송", command=send_servo_angles)  # 각도 전송 버튼
btn_m_a.grid(column=1, row=0, padx=10, pady=5)

# 첫 번째 서보 모터 조절 슬라이더 및 값 표시 레이블
lb_num1 = Label(win, text="서보 1 각도")  # 서보 1 각도 레이블
lb_num1.grid(column=0, row=1, padx=10, pady=5)

sc1 = Scale(win, from_=0, to=180, orient=HORIZONTAL, length=300)  # 서보 1 각도 슬라이더 (0~180도)
sc1.grid(column=1, row=1, padx=10, pady=5)

lb1_val = Label(win, text="0", width=5)  # 서보 1 각도 값 레이블
lb1_val.grid(column=2, row=1, padx=10, pady=5)

# 두 번째 서보 모터 조절 슬라이더 및 값 표시 레이블
lb_num2 = Label(win, text="서보 2 각도")  # 서보 2 각도 레이블
lb_num2.grid(column=0, row=2, padx=10, pady=5)

sc2 = Scale(win, from_=0, to=180, orient=HORIZONTAL, length=300)  # 서보 2 각도 슬라이더
sc2.grid(column=1, row=2, padx=10, pady=5)

lb2_val = Label(win, text="0", width=5)  # 서보 2 각도 값 레이블
lb2_val.grid(column=2, row=2
```



```cpp
#include <Servo.h>  // Servo 라이브러리 포함

// 서보 모터 핀 번호 설정
const int servoPin1 = 9;  // 서보 1 연결 핀
const int servoPin2 = 10; // 서보 2 연결 핀
const int servoPin3 = 11; // 서보 3 연결 핀
const int servoPin4 = 12; // 서보 4 연결 핀

// Servo 객체 생성
Servo servo1;
Servo servo2;
Servo servo3;
Servo servo4;

void setup() {
    // 서보 모터 핀 초기화
    servo1.attach(servoPin1);  // 서보 1 핀 연결
    servo2.attach(servoPin2);  // 서보 2 핀 연결
    servo3.attach(servoPin3);  // 서보 3 핀 연결
    servo4.attach(servoPin4);  // 서보 4 핀 연결

    Serial.begin(9600);  // 시리얼 통신 시작, 9600bps
}

void loop() {
    // 시리얼 통신을 통해 데이터 수신
    if (Serial.available()) {
        String command = Serial.readStringUntil('\n');  // 줄바꿈까지 문자열 읽기

        // 명령을 ':' 기준으로 분리
        int separatorIndex = command.indexOf(':');
        if (separatorIndex != -1) {
            int servoNum = command.substring(0, separatorIndex).toInt();  // 서보 번호
            int angle = command.substring(separatorIndex + 1).toInt();  // 각도 값

            // 서보 번호에 따라 적절한 서보 모터에 각도 값 설정
            switch (servoNum) {
                case 1:
                    servo1.write(angle);  // 서보 1에 각도 전송
                    break;
                case 2:
                    servo2.write(angle);  // 서보 2에 각도 전송
                    break;
                case 3:
                    servo3.write(angle);  // 서보 3에 각도 전송
                    break;
                case 4:
                    servo4.write(angle);  // 서보 4에 각도 전송
                    break;
                default:
                    // 잘못된 서보 번호 처리 (옵션)
                    break;
            }
        }
    }
}
```


